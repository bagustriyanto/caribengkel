<nav class="navbar navbar-secondary navbar-expand-lg menu-navbar" id='menu-navbar' v-if="$menu.length > 0">
    <div class="container">
        <ul class="navbar-nav">
            <li class="nav-item dropdown" v-for="(item, index) in $menu" :key="index">
                <a href="#" data-toggle="dropdown" class="nav-link has-dropdown"><i
                        class="fas fa-clone"></i><span>{{item.title}}</span></a>
                <ul class="dropdown-menu">
                    <li class="nav-item" v-for="(child, index) in item.child" :key="index"><a
                            v-bind:href="'/system'+child.url" class="nav-link">{{child.title}}</a></li>
                </ul>
            </li>
        </ul>
    </div>
</nav>